extends layout 
include mixins/_recipeCard
include mixins/_pagination

block content
    .inner 
        h2 #{title}
        .recipes 
            each recipe in recipes 
                +recipeCard(recipe)

        +pagination(pagination.page, pagination.pages, pagination.count)